import { UserResolver } from '../UserResolver';

describe('UserResolver', () => {
    it('should create a user', async () => {
        // Test logic for creating a user
    });

    it('should get a user', async () => {
        // Test logic for getting a user
    });
});
